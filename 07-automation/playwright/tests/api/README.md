# API Tests

**Status:** Placeholder — populated in Phase 5/7.

Will contain `products-api.spec.ts` and related specs using Playwright's
`request` fixture — the code-based counterpart to the Postman collection in
`../../../../05-api-testing/`, run in CI.
