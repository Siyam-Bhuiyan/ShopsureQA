# GitHub Actions Workflows

**Status:** Placeholder — populated in Phase 11.

Will contain `qa-pipeline.yml`: triggers on push to `main` and on PR, running
API tests (Newman), UI + accessibility tests (Playwright), with an optional
manually-triggered performance job.
